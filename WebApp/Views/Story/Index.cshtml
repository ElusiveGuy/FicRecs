@model StoryIndexViewModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@await Html.PartialAsync("_StoryListPartial", new StoryListViewModel { Fics = Model.Fics, ShowDetailed = Model.ShowDetailed })
